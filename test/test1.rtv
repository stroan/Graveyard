basetype Float "float" = Float Real "$1";

basefunc mulFF :: Float -> Float -> Float "($1 * $2)";

semantic Position a = POS a "POSITION";

vertexFragment :: Position Float -> Position Float;
vertexFragment (POS a) = POS a;